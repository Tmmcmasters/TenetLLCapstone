

export type Landlord = {
    id: number;
    createdAt: string | null;
    modifiedAt: string | null;
    userId: string,
    firstName: string,
    lastName: string,
    email: string,
    fullName: string,
}

export type Request = {
    
}